import React from "react";
import {
  Brain,
  TrendingUp,
  Sparkles,
} from "lucide-react";

const AIExplainability = ({ explanation, summary }) => {
  if (!explanation) return null;

  const features = Object.entries(explanation);

  return (
    <div className="mt-8 rounded-3xl border border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-900 shadow-xl p-6 transition-all">

      {/* Heading */}

      <div className="flex items-center gap-3 mb-2">
        <div className="p-3 rounded-xl bg-indigo-100 dark:bg-indigo-900/30">
          <Brain className="text-indigo-600 dark:text-indigo-400" size={24} />
        </div>

        <div>
          <h2 className="text-2xl font-bold text-slate-800 dark:text-white">
            AI Explainability
          </h2>

          <p className="text-sm text-slate-500 dark:text-slate-400">
            Why the model predicted this result
          </p>
        </div>
      </div>

      {/* Feature Contributions */}

      <div className="space-y-5 mt-6">

        <div className="flex items-center gap-2">

          {index === 0 && (
            <span className="rounded-full bg-yellow-100 px-2 py-1 text-xs font-bold text-yellow-700">
              #1
            </span>
          )}

          {index === 1 && (
            <span className="rounded-full bg-slate-200 px-2 py-1 text-xs font-bold">
              #2
            </span>
          )}

          {index === 2 && (
            <span className="rounded-full bg-orange-100 px-2 py-1 text-xs font-bold text-orange-700">
              #3
            </span>
          )}

          <span className="font-medium text-slate-700 dark:text-slate-300">
            {feature}
          </span>

        </div>

        {features.map(([feature, value], index) => (

          <div key={feature}>

            <div className="flex justify-between mb-2">

              <span className="font-medium text-slate-700 dark:text-slate-300">
                {feature}
              </span>

              <span className="font-bold text-indigo-600 dark:text-indigo-400">
                {value}%
              </span>

            </div>

            <div className="w-full bg-slate-200 dark:bg-slate-700 rounded-full h-3">

              <div
                className="h-3 rounded-full bg-gradient-to-r from-indigo-500 via-blue-500 to-cyan-500 transition-all duration-700"
                style={{
                  width: `${value}%`,
                }}
              />

            </div>

          </div>

        ))}

      </div>

      {/* AI Summary */}

      {summary && (

        <div className="mt-8 rounded-2xl bg-indigo-50 dark:bg-slate-800 border border-indigo-100 dark:border-slate-700 p-5">

          <div className="flex items-center gap-2 mb-2">

            <Sparkles
              size={18}
              className="text-indigo-600 dark:text-indigo-400"
            />

            <h3 className="font-semibold text-indigo-700 dark:text-indigo-400">
              AI Insight
            </h3>

          </div>

          <p className="text-slate-700 dark:text-slate-300 leading-relaxed">
            {summary}
          </p>

        </div>

      )}

      {/* Footer */}

      <div className="flex items-center gap-2 mt-6 text-sm text-slate-500 dark:text-slate-400">

        <TrendingUp size={16} />

        <span>
          Feature contribution percentages are generated by the machine learning model.
        </span>

      </div>

    </div>
  );
};

export default AIExplainability;